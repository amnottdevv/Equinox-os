/* pl1.c — bisect primes line 7: array global[100] + for + index variabel */
int a[100];

int main() {
    int i;
    for (i = 0; i < 100; i++) a[i] = 1;
    printint(a[50]);
    print("\n");
    return 0;
}
